import mongoose from "mongoose";

const courseSchema=new mongoose.Schema({
    name:{
        type:String,
        required,
        
    },
    name:{
        type:String,
        required,
        
    },
    name:{
        type:String,
        required,
        
    },
})

export default mongoose.model